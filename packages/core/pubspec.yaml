name: core
description: Core functionality and utilities
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  services:
    path: ../services

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0